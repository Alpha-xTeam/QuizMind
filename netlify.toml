[build]
  functions = "netlify/functions"
  publish = "src"

[functions]
  node_bundler = "esbuild"
  external_node_modules = []
  timeout = 30

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
